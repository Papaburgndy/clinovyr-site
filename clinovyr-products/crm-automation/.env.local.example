# Local dev: set keys in repo root .env.local (/Clinoyr/.env.local), not here.
# Symlink for CLI: ln -sf ../../.env.local .env.local
# HubSpot key is loaded from configs/client.json.
ANTHROPIC_API_KEY=sk-ant-...
